name: CI

on:
  pull_request:
    branches: [main]

jobs:
  diagnose-content:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Print content validation errors
        run: node scripts/validate-content.mjs
